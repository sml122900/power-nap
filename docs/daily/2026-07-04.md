# 2026-07-04

## 한 일

- 도그푸딩 중 발견한 버그 2건 수정: `__DEV__` 전용 "테스트: 10초" 알람 버튼 추가, 수면 화면 커피 토글 활성 시 텍스트 대비 부족 수정 (theme.ts의 ink/inkSoft로 전환, WCAG AA 4.5:1 이상 확인 — 기존 `amberTextOn`은 약 4.25:1로 기준 미달이라 폐기)
- `npx expo prebuild --android --clean` 후 AndroidManifest.xml에 `RECORD_AUDIO`/`FOREGROUND_SERVICE*` 권한이 계속 나타나는 문제를 추적 — 원인은 app.json의 수동 permissions 배열이 아니라 `expo-audio` 플러그인 자체의 기본 옵션(`recordAudioAndroid`, `enableBackgroundPlayback` 둘 다 기본값 true)이었음. 플러그인 옵션을 명시적으로 꺼서 해결, `aapt dump permissions`로 실제 빌드 APK에서 검증
- BACKLOG.md 신설 — 도그푸딩 후 결정할 항목(학습 스텝 크기/clamp, Phase 4 후보, v1.1/v2 로드맵)을 문헌 근거와 함께 정리, PROJECT.md §5/§7/§8에 상호 참조 추가
- expo-notifications의 `sound: 'default'` 채널 경고 원인 분석 — Android 네이티브가 'default' 문자열을 커스텀 사운드 파일명으로 취급해 발생하는 오탐임을 소스 추적으로 확인, `sound` 키 생략으로 해결(실제 재생 사운드는 수정 전후 동일함을 코드로 증명)
- tsconfig.json이 `expo start`/`expo run:android`를 실행할 때마다 자동으로 되돌아가는 문제의 근본 원인 규명 — `app.json`의 `experiments.typedRoutes`가 꺼져 있으면 Expo CLI가 매번 강제로 include 배열을 되돌리도록 하드코딩되어 있었음. `typedRoutes: true` 활성화로 영구 해결
- `docs/` 문서 구조 스캐폴딩 (daily/decisions/troubleshooting/par-materials)

## 이슈

- 릴리즈 빌드 후 앱 실행 시 `expo run:android`가 매번 Metro 연결을 기다리다 타임아웃되는 현상 — 실제 빌드/설치는 정상 완료되는 것으로 확인, 무해하지만 반복적으로 헷갈림
- Android 알림 채널은 생성 후 사운드 설정을 코드로 재변경할 수 없어, 이미 설치된 기기에서는 `sound: 'default'` 경고가 앱 재설치 전까지 계속 뜰 수 있음 (실기기 검증 대기)

## 다음

- Phase 4(폴리시) 착수 여부 결정 — 다크모드/Dynamic Type/reduced-motion/접근성 라벨/엣지케이스
- 실전 낮잠 도그푸딩 3~5회 진행 후 BACKLOG.md의 학습 스텝/클램프 항목 재확정
