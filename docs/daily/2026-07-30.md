# 2026-07-30

## 한 일

- **릴리즈 AAB 빌드 + Play 데이터 안전 선언 조사**
  - `gradlew bundleRelease` 빌드 검증(서명 CN=SungMin-Lee, `SHOW_TEST_BUTTONS=false`/
    `REVENUECAT_STORE='play'`/versionCode·Name 확인)
  - Play "데이터 유형" 선언용으로 Supabase/Anthropic/RevenueCat 실제 전송 필드
    전수 확인 + SDK 자동 전송(광고 ID·크래시 로그 등) 여부 확인 — `AD_ID` 권한 자체가
    매니페스트에 없어 광고 ID 미수집 확정
  - 후속 질문(익명 계정 생성 시점)에서 트리거가 "AI 분석 동의" 하나가 아니라
    "마이페이지 → 결제내역 → 구매 복원" 버튼으로도 독립적으로 발생함을 코드로 발견

- **Play 스토어 등록정보 준비**
  - `feature-graphic-1024x500.png` 신규 생성(네이비 배경 + 셰브런 마크 + PowerNap
    워드마크)
  - `STORE_LISTING.md` 신규 작성(앱 이름/짧은 설명/자세한 설명/스크린샷 8장 캡션·순서/
    카테고리) — 1차 피드백으로 위젯 동작 설명(앱이 안 열린다는 오기술)·알람 시간 조정
    경로(설정 화면이 아니라 마이페이지)를 코드 대조 후 정정, 다크모드 3지선다 반영
  - 검증 4종 통과 후 커밋·push

- **브랜드 마크 전면 교체 (셰브런 → P 워드마크)**
  - 하늘색 후보 3개를 기존 팔레트(E6F4FE)와 같은 색조에서 명도만 달리해 실측
    대비(WCAG)와 함께 제시 → `#1392EC` 확정
  - 런처 아이콘/adaptive icon 3종/스플래시/스토어 아이콘/피처 그래픽 7개 자산 재생성,
    `DESIGN_HANDOFF.md`에 브랜드 마크 전용 색 예외 조항 기록, `app.json` adaptiveIcon
    배경색 정정
  - 작업 중 VS Code 창이 `android/` 폴더를 워크스페이스로 열고 있어 `prebuild --clean`이
    막힌 것 발견·해결(이 세션 자체가 그 창의 자식 프로세스였다는 걸 프로세스 트리로
    확인 — 잘못 닫으면 세션이 같이 죽는 상황이었음)
  - 실기기(Galaxy S24+) 완전 삭제 후 재설치로 런처 아이콘(흰 배경+하늘색 P, 원형
    마스크에서 안 잘림)·스플래시 새 마크 확인, `CLAUDE.md`의 stale 지뢰 항목(옛
    파일명 참조) 정정

- **Play 스토어 태블릿 스크린샷 16장**
  - 로컬에 Android cmdline-tools + system image(android-36, x86_64) 신규 설치,
    7인치(1080×1920)/10인치(1440×2560) AVD 2종을 Play 요구 비율(9:16)에 맞춰 커스텀
    해상도로 생성
  - 사전 레이아웃 점검에서 sw600dp+ 화면 하단 35~45%가 빈 공간으로 남는 이슈 발견 →
    사용자 판단으로 "깨짐 아님, 그대로 촬영" 결정
  - 더미 낮잠 기록 5개 + AI 분석 리포트(한국어)까지 포함한 8장×2세트 촬영 — 과정에서
    호스트 메모리 부족으로 에뮬레이터 2개 동시 구동 시 zygote 크래시 확인(순차 실행으로
    전환), `expo-alarm-module`의 null Alarm 방어 누락으로 인한 실제 NPE 크래시도
    발견(비정상 테스트 경로에서만 재현 — "10초 알람 체험"으로 안전하게 우회)
  - 16장 전부 RGBA→RGB 변환(알파 채널 랜드마인 재발 방지) 후 커밋, `BACKLOG.md`에
    태블릿 반응형 레이아웃 갭·NPE 이슈 기록

- **위젯 선택 화면 미리보기 추가**
  - `previewLayout`(API 31+, 기존 레이아웃 XML 재사용)과 `previewImage`(API 24~30
    폴백, 팔레트 동일하게 렌더링한 정적 PNG 신규 생성)를 `widget_info_*.xml`에 추가
  - 실기기 검증 중 M/L 위젯의 힌트 텍스트가 버튼 폭을 넘어 잘리는 걸 실측(Pretendard
    11sp 기준 93dp 가용폭에 129dp 문구)으로 확인 — 커밋 후 별도 이슈로 분리
  - 후속 확정안(M/L 힌트 제거, 위젯 이름 "1/2/3버튼 위젯"으로 통일) 실기기 최종 검증
    완료 — 위젯 선택 화면에서 새 라벨 3종·미리보기(M/L은 힌트 없이 제목만 중앙 정렬)
    확인, M 위젯을 홈 화면에 실제 배치해 텍스트 안 잘림 확인, 탭 → 앱 열림 → 알람
    즉시 예약(수면 화면 진입)까지 회귀 없음 확인 → `feat: simplify widget labels and
    hints`로 커밋·push, `REVIEW_NEEDED.md` 영어 라벨도 함께 갱신

- **수면 화면 "그만 자고 일어나기" 버튼 반응 없음 — 재현 조사**
  - 위젯 검증 중 만든 테스트 낮잠을 취소하려는데 버튼이 여러 번 눌러도 무반응 —
    `onCancel` 코드(`app/sleep.tsx`) 자체엔 문제 없어 보여 실기기에서 좌표를 의심
  - `uiautomator dump`가 카운트다운 애니메이션 때문에 계속 "could not get idle
    state"로 실패해 `bounds`를 못 구함 → 스크린샷을 Pillow로 직접 픽셀 스캔해서
    버튼 테두리 위치를 찾아냄
  - 원인 확정: **코드 버그가 아니라 좌표 환산 실수** — 표시 스크린샷(923×2000)과
    실제 해상도(1080×2340) 배율(1.17배)을 매번 다시 계산하다 이번엔 완전히 잘못
    짚어 시스템 내비게이션 바 근처를 계속 누르고 있었음. 정확한 좌표(픽셀 스캔으로
    구함)로 누르니 즉시 취소되고 정상적으로 홈 화면(온보딩)까지 복귀

## 이슈

- adb `input swipe`로 위젯 드래그·앱 롱프레스 좌표를 잡을 때 표시 스크린샷과 실제
  물리 픽셀 배율(예: 1440x2560 기기에서 1.28배) 환산을 반복적으로 틀려 여러 번
  재시도 — `uiautomator dump`로 정확한 `bounds`를 구하는 방식이 훨씬 안정적이었음
- 태블릿 에뮬레이터에서 위젯 드래그 배치가 `input draganddrop`으로도 불안정하게
  실패하는 경우가 많았음(성공률 낮음, 정확한 원인 불명 — launcher의 드래그 인식
  타이밍 문제로 추정)
- 실기기 화면이 꺼짐/잠금 반복으로 `wm dismiss-keyguard` + `KEYCODE_WAKEUP` 조합이
  필요했고, 세션 사이 시간 간극이 크게 벌어지면 폰이 다른 앱(카메라·유튜브 등)을
  띄운 상태로 남아있어 매번 홈 화면 상태부터 재확인해야 했음
- 실기기 좌표 자동화는 애니메이션이 계속 도는 화면(카운트다운 등)에서
  `uiautomator dump`가 구조적으로 못 미더움 — 스크린샷 픽셀 스캔이 유일한 대안이었음

## 다음

- **Play 스토어 폰 스크린샷 8장** — 사용자가 이미 직접 촬영해뒀음(레포 밖에 보관
  중), 다음에 전달받으면 `assets/store/screenshot-0N-*.png`로 반입만 하면 됨.
  (오늘 세션 중 이 파일들이 레포에 없는 걸 보고 "새로 촬영이 필요하다"고 잘못
  판단했었음 — 사실이 아니었음, 기록 정정)
- `expo-alarm-module` null Alarm 방어 누락은 BACKLOG.md에 기록된 대로 v1.1 알람
  네이티브 패치 작업 때 병행
- 태블릿 반응형 레이아웃(하단 여백) 개선도 v1.1 대상으로 보류 중
