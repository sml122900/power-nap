# 2026-07-27

## 한 일

- **온보딩 4장 화면 목업 설계·구현** — 텍스트만 있던 온보딩에 각 실제 화면(홈 3버튼·
  기상루틴·AI 리포트·위젯)을 본뜬 정적 목업 + glow 하이라이트 추가. 실제 컴포넌트
  이식이 아니라 도형·색·타이포로 형태만 재현(상태/훅/네비게이션 없음). 구현 전
  Artifact로 목업 미리보기를 만들어 슬라이드별 구성·하이라이트 방식을 먼저 확인받음
  (아이콘 예외 범위, 슬라이드1 순차 glow, 슬라이드2 미션 칩 추가는 전부 사용자 확정
  사항). 4개 슬라이드 모두 같은 outer-glow-ring primitive(`withRepeat`+`withSequence`,
  sleep.tsx의 breathDot 패턴 재사용)를 공유하고 orchestration만 다르게 함.
  - 앱 전체 아이콘 0개 원칙(DESIGN_HANDOFF.md)에 온보딩 슬라이드2(기상루틴) 한정
    예외로 `lucide-react-native` 도입 — 배럴 import(`import { X } from 'lucide-react-native'`)
    시 아이콘 1000개+가 전부 번들에 딸려와 모듈 수 1925→3678, 번들 6.4MB→8.2MB로
    뛰는 걸 `expo export`로 실측, 서브패스 import(`lucide-react-native/icons/x`)로 교정.
  - jest에서 reanimated 4.5의 실제 워클릿 네이티브 모듈이 크래시(`loadUnpackers`) —
    공식 `react-native-reanimated/mock`도 같은 초기화 경로를 타서 못 씀, 최소 수동
    mock으로 대체.
  - 커밋: `eee7870`(목업 구현), `a65a148`(lucide 배럴 임포트 지뢰 기록).
- **실기기 릴리즈 빌드 검증** (`react-native-svg` 첫 도입이라 중요) — `expo prebuild
  --clean` 후 config plugin 6종(FullScreenAlarmIntent/AlarmStopVibrationFix/
  AlarmForegroundStartFix/HomeScreenWidgets/RemoveUnusedDebugEntries/ReleaseSigning)
  전부 정상 적용 확인(매니페스트 권한·signingConfig·위젯 리시버·알람 패치 마커),
  `react-native-svg` 릴리즈 빌드 링킹·크래시 없음 확인. 실기기에서 슬라이드2의
  3단계 칩이 가로 배치 시 프레임 폭(128px)을 넘어 좌우가 잘리는 버그 발견 →
  세로 배치로 수정(`9ee1a9b`). 다크/라이트 양쪽, logcat FATAL/SVG 에러 0건 확인.
- **더미 낮잠 기록 생성 로직 결함 발견·수정** — 스토어 스크린샷용으로 "더미 기록
  5개" 생성 후 AI 분석을 돌렸더니, 리포트 본문이 "모든 기록의 설문 항목이 '중간'
  으로 동일하고... 낮잠 시각도 매번 같아 패턴을 찾기 어렵다"고 그대로 지적 —
  `onSeedDummyRecords`가 offsetMinutes(25분 고정)·survey(전부 mid 고정)를 매번
  똑같이 채우던 게 원인. 모드별 유효 범위 안에서 offsetMinutes/survey/시각을
  다양하게 주도록 수정(`fa2d6b5`). 기존에 쌓여있던 편차 없는 더미 기록 15개를
  실기기에서 하나씩 삭제 후 새 로직으로 재생성, 재분석 결과 자연스러운 리포트
  확인.
  - 재분석을 위해 Supabase에 dev 크레딧 1개를 직접 부여(`credit_events`에
    `reason='refund'`, delta=+1 insert — `reason` CHECK 제약이 `purchase`/
    `weekly_free`/`analysis`/`refund`만 허용해 `dev_grant` 같은 임의 값은 불가).
    잔액은 캐시 컬럼(`credits.balance`) + 트리거로 자동 반영되므로 이벤트 insert만
    하고 잔액 컬럼은 직접 안 건드림. weekly_free 타임스탬프도 안 건드림.
    대상 uid는 당일 `analyses` 테이블에서 실제 분석이 찍힌 행으로 특정(로컬
    AsyncStorage 접근 없이 서버 쪽 근거로 확인).
- **Play 스토어 등록용 아이콘 512×512 재생성** — 기존 `assets/store-icon-512.png`가
  RGBA(알파 완전 불투명이라 육안으로는 멀쩡)였던 걸 발견, Play는 알파 채널이
  있으면 업로드 자체를 거부함. 스플래시(`splash-icon.png`, 흰 셰브런)·런처
  adaptive icon(`android-icon-foreground.png`, 그라데이션 블루 셰브런) 소스를
  그대로 재사용해 알파 없이 두 옵션 합성:
  - `assets/store/store-icon-512-navy.png` — 스플래시와 동일한 `#12172A` + 흰 셰브런(추천 — 대비 강해 스토어 탐색 화면에서 눈에 띔)
  - `assets/store/store-icon-512-light.png` — 런처/iOS 아이콘과 동일한 `#E6F4FE` + 그라데이션 셰브런(설치 아이콘과 일관성)
  기존 알파 있는 파일 삭제, 코드/`app.json` 참조 없음 확인 후 진행.
  커밋: `c96513f`(아이콘 자산), `fc5e76a`(알파 채널 지뢰 기록).

## 이슈

- adb 기기 연결이 중간에 끊김(USB 케이블 문제로 추정) — 재연결로 해결.
- Windows Gradle 빌드 중 `createBundleReleaseJsAndAssets`가 간헐적으로 파일쓰기
  오류(`node_modules_exporouter_assets_reactnavigation_elements_clearicon.png`)로
  실패 — `android/app/build/generated/{res,assets}/react` 캐시 삭제 후 재시도로
  매번 해결(원인은 안 팜, 재현 패턴만 기록해둘 가치 있음).
- 실기기 스크린샷에 원인 불명의 검은 오버레이가 여러 장 찍힘 — 삭제 확인
  다이얼로그 버튼 위치와 겹쳐 오조작 위험이 있어 작업 중단하고 사용자에게 확인
  요청, 카카오톡 통화/PIP 창으로 추정(사용자가 직접 종료 후 재현 안 됨).
- adb 스크린샷 좌표 계산에서 표시 배율(923×2000 표시 / 1080×2340 실제, ×1.17)을
  누락한 탭이 몇 차례 있었음 — 스크린샷으로 매번 확인하는 방식으로 대응.

## 다음

- **[BACKLOG]** 앱 재설치 시 Supabase 익명 계정이 새로 생성되면서 주 1회 무료
  AI 분석이 리셋될 가능성 — 사실이면 실사용자도 재설치로 무료 분석을 무한
  반복할 수 있는 구조적 문제. 이번 세션엔 고치지 않고 기록만 함(사용자 지시).
  사실 여부 확인 후 v1.1에서 기기 식별자 기반 제한 또는 선택적 로그인으로
  다룰 후보.
- expo-doctor의 9개 패키지 patch 버전 드리프트는 여전히 미해결(기존부터 알려진
  사항, 이번 세션과 무관 — v1.1 시작 시 `expo install --fix`로 처리 예정).
- 온보딩 4장의 다크/라이트·작은 기기 레이아웃은 이번 세션에 실기기로 확인
  완료. 스토어 스크린샷용 더미 데이터·아이콘은 준비 끝났고, 실제 Play Console
  등록 시 이번에 만든 두 아이콘 중 최종 선택 필요.
