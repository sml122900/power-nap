# 2026-07-11

## 한 일

- **알림 권한(POST_NOTIFICATIONS) 거부 시나리오 실기기 검증 결과 반영**
  - 실기기 검증 결과: Android는 소리·진동이 권한과 완전히 무관하게 항상 발화, 권한이
    실제로 좌우하는 건 화면 자동 점등(풀스크린 인텐트)뿐임을 확정 — PROJECT.md §4에
    4행 표로 기록
  - 수면 화면 안내 문구를 검증 결과에 맞게 최종 확정 + "권한 허용하기" 버튼 신규
  - 딥링크를 `Linking.openSettings()`(앱 일반 설정)에서 `expo-intent-launcher`의
    `ACTION_APP_NOTIFICATION_SETTINGS`(알림 설정 화면 직행, 실패 시에만 폴백)로 정밀화
  - 설정에서 돌아왔을 때(`AppState` 복귀) 권한을 다시 조회해 안내를 자동으로 숨기도록
    개선 — `ActiveNap.notificationPermissionGranted`는 낮잠 시작 시점 고정값이라 별도
    실시간 상태로 관리

- **개인정보처리방침 "서버 데이터 삭제" 기능**
  - `delete-my-data` Edge Function: 인증된 유저의 `auth.users` 행 삭제 → 기존 FK
    cascade로 `public.users`/`credits`/`credit_events`/`analyses`가 한 트랜잭션에서
    함께 정리됨(새 마이그레이션/RPC 불필요 — 상세는 아래 결정 기록 참고)
  - 설정 화면에 2단계 확인(`Alert.alert` 안내 → 최종 확인, 크레딧 잔액 있으면 경고
    문구) + 삭제 성공 시 로컬 AI 동의 상태/캐시 초기화
  - 이미 레포에 존재하던(이번 세션에 작성하지 않은) `PRIVACY_POLICY.md` 초안을 발견해
    커밋에 포함 — §5가 이번 구현과 정확히 일치
  - 실배포 Edge Function에 대해 통합 테스트 4건(인증 없이 401, 삭제 후 4테이블 전부
    빈 결과, 타 유저 데이터 격리, 삭제된 유저 JWT 재호출 시 자연스러운 401) 실행 검증

- **알람 해제 미션(명언 타이핑) 기능**
  - 설정 화면 "알람 해제 미션" 토글(기본 OFF) — ON이면 알람 발화 시 기존 슬라이드/
    롱프레스 해제 화면 앞에 명언 타이핑 화면이 먼저 나오고, 뒷단(체크리스트/설문)은
    그대로
  - `useNapWatchdog`의 라우팅 판정을 `resolveNapRoute(nap, missionEnabled, now)` 순수
    함수로 분리 — jest로 직접 테스트 가능해짐(AsyncStorage/React 없이)
  - 알람음(iOS)·햅틱 시작 로직을 `app/alarm.tsx`에서 `src/useAlarmPlayback.ts` 훅으로
    추출해 미션 화면과 공유(모듈 레벨 재생 가드도 함께 이전)
  - 명언 15~20개(한/영 각 18개)는 저작권 리스크 회피를 위해 전부 자체 작성. 입력
    대조는 공백/구두점 제거+소문자화 후 비교, 3회 연속 실패 시 더 짧은 문구로 교체
  - 수학 문제 미션은 "알라미 시그니처라 차별화 안 됨 + 낮잠 앱 톤과 불일치"로 배제,
    근거를 BACKLOG.md에 명시적으로 기록

- **"파워냅이란?" 정보 화면**
  - 홈 화면에 아이콘 없는 텍스트 링크 → 스크롤 화면, 5개 섹션 카드(왜 10~20분인가 /
    언제 자야 하나 / 커피냅이 효과적인 이유 / 깨어난 뒤 30초 / 낮잠은 밤잠을 대신하지
    않는다)
  - 수치는 BACKLOG.md 문헌 근거 섹션을 그대로 옮겨 씀(새로 조사하지 않음), 장기
    건강효과·진단/치료 표현은 배제(AI 분석 프롬프트의 "제외할 것"과 동일 기준)

- **세 브랜치 병합(`about-powernap`, `mission-alarm`) → `main`**
  - 순서: `about-powernap → main`(충돌 없음) → `main → mission-alarm`(BACKLOG.md/
    PROJECT.md 헤딩만 충돌, 본문은 자동 병합) → `mission-alarm → main`(충돌 없음)
  - 병합 직후 `resolveNapRoute` 라우팅 회귀를 jest로 재확인(107개 = 기존 88 +
    missionQuotes 8 + useNapWatchdog 6 + store 5, 정확히 합산됨을 확인)
  - `main`에서 4종(tsc/expo-doctor/expo export/jest) 재검증 통과
  - `expo prebuild --clean` → `gradlew clean assembleRelease`(20분대) → 실기기 재설치,
    이후 사용자 요청으로 재확인 빌드 1회 더(코드 변경 없어 prebuild 생략, 1분 38초)

## 이슈

- adb 기기 인증이 릴리즈 빌드 사이사이 계속 풀렸다(`unauthorized`/미연결) — 케이블
  재연결·화면 잠금 해제로 매번 해결됐고 코드 문제는 아니었지만, 이번 세션에만 4~5회
  반복돼 빌드-설치 루프의 체감 마찰이 컸다
- expo-router 타입드 라우트가 신규 라우트(`/mission`, `/about`) 추가 직후엔 tsc에서
  안 잡힌다 — `expo start`를 잠깐 띄워 번들 요청을 한 번 실제로 보내야
  `.expo/types/router.d.ts`가 재생성됨(CLAUDE.md 지뢰 목록에 이미 있던 규칙을 이번에도
  그대로 재확인)
- `mission-alarm`을 백그라운드 프로세스로 잠깐 세워뒀던 Metro가 `TaskStop` 이후에도
  완전히 안 죽어, 나중에 포트 8080에서 여전히 응답하는 걸 발견 — PowerShell로 리스닝
  프로세스를 직접 찾아 강제 종료해야 했다

## 다음

- 실기기 기능 검증 5건 대기: 권한 안내(딥링크·자동 숨김), 서버 데이터 삭제, 알람
  해제 미션 전체 흐름, "파워냅이란?" 화면, 알림 권한 거부 상태의 `/alarm` 직행
- `mission-alarm`/`about-powernap` 브랜치 정리(삭제) — main 병합 완료, 사용자 확인 후
- `PRIVACY_POLICY.md` 법률 검토·영어판, `REVIEW_NEEDED.md` 신규 항목(미션 문구·about
  화면 문구) 원어민 검수
