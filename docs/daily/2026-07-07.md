# 2026-07-07

## 한 일

- 릴리즈 빌드 재배포 워크플로 정리: `ANDROID_HOME`/`JAVA_HOME`이 bash 셸에 없어 `expo run:android`가
  기기를 못 찾던 문제 해결(PowerShell adb 경로와 별개로 bash용 환경변수 명시), 이후 세션 전체에서
  재사용
- 낮잠 히스토리 화면 신설 (BACKLOG v1.1 항목을 사용자 요청으로 코드 동결 중 선반영) — `NapRecord`
  읽기 전용 목록, `format.ts`에 `formatKoreanDateTime` 추가
- `SHOW_TEST_BUTTONS` 플래그(`src/config.ts`) 도입 — 기존 `__DEV__` 게이트를 대체해 도그푸딩 중엔
  릴리즈 빌드에서도 테스트 알람 버튼이 보이도록 하되, 출시 전 체크리스트로 관리(CLAUDE.md에 규칙 추가)
- 도그푸딩 버그 3건 수정: 알림 채널 ID 버전업(`alarm`→`alarm-v2`, Android가 채널 생성 후
  importance/sound를 재설정 못 하는 문제), 알람 화면 롱프레스 해제(`Pressable.onLongPress`→
  `Gesture.LongPress()`), 수면 화면 커피 토글 프리뷰 텍스트
- **Android 네이티브 알람 전환** — 원래 Phase 5 예정이었으나 실기기 테스트에서 백그라운드/잠금
  상태 알림이 진동만 나고 소리가 없는 것을 발견해 앞당김:
  - Notifee를 조사하다 **2026-04-07에 저장소가 아카이브됐다는 사실을 직접 확인**(공식 후속 권장은
    `react-native-notify-kit` 포크 또는 `expo-notifications`). 대안 비교(`expo-alarm-module`,
    `expo-alarm` 등) 후 `expo-alarm-module` 채택 — 상세는 `docs/decisions/` 참고
  - 브랜치 `spike/native-alarm`에서 먼저 위험만 검증: New Architecture 릴리즈 빌드 링크,
    `STREAM_ALARM`이 무음모드+미디어볼륨0을 실제로 관통하며 연속 재생되는지 실기기로 확인
  - JS 연동(소리 책임을 Android=네이티브/iOS=기존 expo-audio로 이관) 후 실기기 전 항목(백그라운드/
    잠금/무음 연속 발화, 해제, 낮잠취소, 커피토글 재예약, 2연속, 강제종료 상태 발화) 통과
  - 롱프레스 해제가 또 완전히 깨진 걸 발견 → 트러블슈팅(아래 참고)으로 두 단계에 걸쳐 해결
  - `main`에 병합, CLAUDE.md/PROJECT.md/STATUS.md/BACKLOG.md 갱신, 릴리즈 재배포(도그푸딩 3기
    기준 버전)
- **Phase 4-2: 학습 모델 v2 + 커피냅 3모드** — "오프셋 4버킷" 모델을 "기준시각+소요시간"으로 개편:
  - `latency{fast,slow}`(0~20분) + `caffeineOnset`(15~35분, 카페인 발현시간 문헌 근거 포함)로
    데이터 모델 교체, `coffee: boolean`을 폐지하고 `NapMode`에 `'coffee'` 3번째 값 추가
  - 홈 화면에 커피냅 버튼 → 인라인 칩(방금/5분전/10분전/직접입력) 확장 UI, 커피가 이미 돌고 있는
    경우 now+10분 보정 로직(`computeCoffeeAlarmAt`)
  - 후기 화면의 3버튼 라벨이 실제 적용 스텝 대신 하드코딩된 "5분"을 보여주고 있던 버그를 겸사겸사
    발견해 수정(Phase 4-1 스텝 개편 이후 라벨이 안 갱신됐던 것)
  - jest 20개(기존 11 + 신규 9: 마이그레이션 v1/v2→v3, 3모드 학습 독립성, 커피 보정 로직)
  - 브랜치 `phase-4-2`에 커밋·푸시, 릴리즈 빌드 배포까지 완료. 실기기 검증은 진행 중

## 이슈

- 실기기 검증 중 "소리가 전혀 안 남" 오탐 발생 — 실제 원인은 알람이 이미 재생 중일 때 물리
  볼륨키를 누르면 안드로이드가 그 입력을 알람 스트림 쪽으로 돌려버려서(OS 기본 동작) 사용자가
  "미디어 볼륨 0으로" 누른 게 알람 볼륨을 0에 가깝게 깎아버린 것이었음. 코드 문제가 아니었다 —
  CLAUDE.md 지뢰 목록에 기록
- `expo-alarm-module`은 STREAM_ALARM 연속재생은 확실히 해결해주지만 풀스크린 인텐트/화면
  자동점등은 기본 제공하지 않음 — 다음 단계로 별도 네이티브 패치 예정(PROJECT.md §4 "알려진 한계")
- Phase 4-2 릴리즈 빌드 실기기 검증 결과 아직 미확인 (STATUS.md 체크리스트 참고)

## 다음

- Phase 4-2 실기기 검증 완료 후 `phase-4-2` → `main` 병합 여부 결정
- 풀스크린 인텐트 + 화면 자동점등 네이티브 패치(별도 브랜치, 다음 지시 대기)
- 도그푸딩 계속하며 학습 스텝/clamp 재확정 여부 관찰
